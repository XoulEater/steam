const express = require('express');
const adminLogisticSchema = require('../models/adminLogisticSchema');

const router = express.Router();


//TODO: Endpoint de funciones de admin-logistic

module.exports = router;
